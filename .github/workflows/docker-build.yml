name: Docker Build
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      
      - name: Docker Build
        run: docker build . -t payment-gateway:${{ github.run_id }}
